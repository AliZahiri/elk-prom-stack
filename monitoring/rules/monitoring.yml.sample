groups:
  - name: monitoring-baseline
    rules:
      - alert: PrometheusTargetDown
        expr: up == 0
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: A Prometheus scrape target is unavailable
