# Local development values only. Replace every placeholder before use.
ELASTIC_USER=elastic
ELASTIC_PASS=change-this-local-password
FILEBEAT_PASS=change-this-filebeat-password

# Filebeat changes the host package state; keep it opt-in.
INSTALL_FILEBEAT=false
